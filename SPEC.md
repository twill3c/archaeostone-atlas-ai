# SPEC.md — archaeostone-atlas-ai(ArchaeoStone Atlas AI)

構想書: `docs/ArchaeoStone_Atlas_AI_V1.0_Complete_Implementation_Spec.md`
本 SPEC は構想書を**実測で上書きした**版である。食い違う箇所は §2 に列挙する。

---

## 1. 目的

日本の**黒曜石原産地**を、その足もとの地質(産総研 20 万分の 1 日本シームレス地質図 V2)と
重ねて配り、「原産地の地質は、無作為な陸地点と比べてどれだけ偏っているか」を測って見せる。
あわせて、ヒスイ(糸魚川市集成表)と全国遺跡報告総覧の文献分布を、
**根拠の階段(Evidence Ladder)ごとに分けて**同じ地図に載せる。

構想書の目的は「石材の出土分布のアトラス」だったが、**出土分布を配ることができない**
(§2.1)。配れる源の上に主題を置き直した結果が上記である。

---

## 2. 構想書との差分(すべて 2026-09-08 実測)

### 2.1 中核データ源が二つとも再配布不可(S1・HC-238)

| 源 | 到達 | 再配布 | 実測した根拠 |
|---|---|---|---|
| 糸魚川市 ヒスイ出土情報集成表 | ✅ HTTP 200 / 230,912 B | ❌ 不可 | 同市「著作権・リンク等について」(2025-02-12 更新): 著作権は原則として糸魚川市に帰属。「私的利用」または「引用」を除き**無断で転載、複製、改変など利用をすることはできません** |
| 明治大 COLS 黒曜石分析リスト PDF 2 本 | ✅ HTTP 200 | ❌ 不可 | 明示ライセンス無し。構想書 §3.7.2 自身が「自動的にオープンデータ扱いしない」と書く |

**到達できることと、配れることは別の検査である。** どちらも素直に落ちてきて中身も良質だが、
権利の壁はデータ URL から三クリック離れた著作権ページにしかなかった。

**処置**: 構想書 §3.3.5 の初期状態(`redistribution: review_required` / `raw_commit: false`)と
§4.4 の fail-closed に従い、**どちらも公開ビルドへ同梱しない**。
ヒスイは取得器・解析器・検査だけをリポジトリに置き、公開するのは**集計統計のみ**とする(§6.3)。
COLS は参照リンクと方法論の引用に限る。

### 2.2 ジャパンサーチ検索 API は robots.txt が禁じている(HC-235)

`https://jpsearch.go.jp/robots.txt` に、構想書 §3.6.1・§41 が名指しする当の URL が並ぶ:

```
Disallow: /api/item/search/jps-cross
Disallow: /api/item/search-so/jps-cross
Disallow: /data/
Disallow: /rdf/
Disallow: /entity/
Disallow: /term/
Allow:    /rdf/sparql-explain/
```

**処置**: SRC-JPSEARCH は V1.0 スコープ外。迂回路は取らない。

### 2.3 OAI-PMH の題名に石材はほとんど出ない(全件走査で 0.09%)

**先頭 25 ページ 2,650 件の標本では 0 件だったが、全 3,128 ページ 331,562 件を
走査すると出る。** 標本の 0 件は「無い」ではなく「率が低くて当たらなかった」だった
(0.09% なら 2,650 件の期待値は 2.5 件)。以下は全件走査の実測(2026-09-10)。

| 石材 | 題名に現れた件数 | 率 |
|---|---|---|
| 黒曜石・黒耀石 | **299** | 0.0940% |
| ヒスイ・翡翠・硬玉 | **218** | 0.0685% |
| 水晶・石英 | 42 | 0.0132% |
| サヌカイト・讃岐岩 | 33 | 0.0104% |
| チャート | 21 | 0.0066% |
| 碧玉・ジャスパー | 19 | 0.0060% |
| 蛇紋岩 | 7 | 0.0022% |
| 瑪瑙・めのう | 6 | 0.0019% |

分析方法の語も同様に少ない: 原産地 162 / 産地推定 112 / 蛍光X線 98 /
蛍光Ｘ線 4 / 中性子放射化 4 / ICP-MS 0。

発掘調査報告書の題名は**遺跡名と叢書名**が主なので、石材はまれにしか現れない。
現れるのは「鷹山遺跡群」「長野県黒耀石原産地遺跡分布調査報告書」のような、
**原産地そのものを調査した報告書**である。

`oai_dc` レコードに `dc:subject` / `dc:description` / `dc:coverage` / `dc:rights` は
**1 件も無く**、構想書 §3.2.3 の取得フィールド一覧は実物と一致しない。

**処置**: Nabunken は「**文献の分布**」として使う。299 件の黒曜石関連報告書は
市区町村コードつきで得られるので、原産地の頁から当該地域の文献へ辿れる。
ただしこれは**石材が出土した遺跡の分布ではない** —— 題名にその語が現れた
報告書の分布である。画面でもそう明記する。

構想書 §12(報告書 PDF 全文からの NLP 抽出)は、本文の著作権が発行自治体ごとに
異なる(Nabunken 利用案内)ため V1.0 では行わない。

### 2.3.1 源の公表する総数が、源自身の返すレコード数と合わない

| 値 | 件数 |
|---|---|
| 全 3,128 ページを解析して得たレコード | **331,562** |
| `resumptionToken` の `completeListSize` | **312,794** |
| 差 | 18,768 |

差は **junii2 形のレコード数と完全に一致する**。

```text
oai_dc 299,325 + メタデータ無し 13,469 = 312,794  ← completeListSize と一致
junii2  18,768                                    ← 数えられていない
```

**公表される総数は論文レコード(junii2)を数えていない。**
だから `completeListSize` を件数オラクルに使うと、
**junii2 を取りこぼした解析器と、正しい解析器が区別できない** —— どちらも
312,794 になる。取りこぼしを検出するつもりの数が、取りこぼしを隠す。

出荷形には三形の内訳を必ず載せ、`completeListSize` は「源の公表値」として別に持つ。

### 2.4 実測で判明した、使える構造

- **`setSpec` が全国地方公共団体コードを持つ。** 2,650/2,650 件が最低 1 個を持ち、
  2 桁 = 都道府県 / 5 桁 = 市区町村。**地理コード化は無料で付いてくる**
- `completeListSize` = **312,794**
- 1 応答に **三形が混在**する: `oai_dc` 2,227 / `junii2` 150 / **メタデータ無し 273(10.3%)**。
  最後のものは `Identify` の `deletedRecord: transient` に対応する
- `dc:identifier` は**無型で多値**(1 レコード平均 6.0 個)。URL・DOI・NCID・叢書名・巻号・
  日付が同じ要素に入る

### 2.5 GSJ 地点 API は「無い」を三通りで返し、うち一つが HTTP 500(HC-235)

| 地点 | 応答 | 意味 |
|---|---|---|
| 陸域のポリゴン上 | 200 + 全項目 | 地質あり |
| 諏訪湖上 36.0450,138.0830 | 200 + `symbol:null` / `title:","` / `value:"000000"` | 図郭内・ポリゴン無し |
| 琵琶湖上 35.25,136.08 / 太平洋沖 34.5,140.0 / タイ 12.0,100.0 | **HTTP 500 + 本文 0 バイト** | 被覆外 |

**500 は障害ではなく「被覆外」という答えなので再試行してはならない。**
座標順は `point=lat,lng`(逆順は `code:102` のパラメータエラーで検出できる)。
`title` は `formationAge_ja + "," + lithology_ja` の連結なので、両方 null のとき `","` になる。
**`title` を解析に使わない。**

### 2.6 地理院の利用規約は構想書の記載より新しい

構想書 §3.5.2 は「政府標準利用規約」を前提に書かれているが、
`https://www.gsi.go.jp/kikakuchousei/kikakuchousei40182.html` は
**令和7年11月20日改正**で **公共データ利用規約(第1.0版)(PDL1.0)** に変わっている。
出典表記「出典:国土地理院ウェブサイト (URL)」、編集・加工した場合はその旨を別途記載。

### 2.7 ヒスイ集成表の実物(構想書 §3.3.3 との差)

`sha256 77a48b13077637f8e16c4e6e5b7394434fce053d251bb329d36aa7ad3eec1884` / 230,912 B。

- **14 シート = 9 データシート + 4 文献シート + 註**。データ行 **計 572 行**
- **収録は 9 県のみ**(北海道・青森・岩手・福島・埼玉・東京・千葉・茨城・新潟(上中越))。
  構想書 §14.1 の「北海道〜九州の出土分布」は**この表からは作れない**
- **註と実体が食い違う。** 註は調査担当者を 9 県分挙げるが、そこにある**栃木県のシートは無く**、
  逆に**新潟県のシートは註に無い**
- **列構成が 3 通り**(11 / 12 / 13 列)。しかも 12 列どうしで中身が違う ——
  北海道・青森の 12 列目は `伴出土器`、埼玉・東京・千葉の 12 列目は `石材`。
  **位置で読むと黙って混ざる。列は見出し名で対応づける**
- **見出し行の位置が一様でない。** 新潟県だけ 0 行目が注記で、見出しは 1 行目
- **13 行が背景色でのみ「製作遺跡」を示す。** 註は「緑の欄」と書くが実体は
  RGB(255,255,153)。この 13 行は `点数` セルの型が `BLANK`(書式あり・値なし)である
  13 行と**完全に一致**する(内部の非循環照合になる)
- **`点数` は 343 件が数値・215 件が空・1 件が文字列 `６？`・13 件が BLANK。**
  註 2「不明な箇所は空欄とした」より、**空欄は 0 ではなく不明**。
  埼玉・東京・千葉は 3 県まるごと点数の記録が無い
- **`文献` 列の意味がシートごとに違う。** 北海道・青森は文字列(`久保1977`)、
  他は文献シートへの番号参照。新潟では区切りが `.` なので Excel が
  `20・3` を数値 `20.3` に変えている(`2.8他` は文字列で残っている)

---

## 3. データ源(V1.0)

| ID | 源 | 到達 | 再配布 | 出典表示 | V1.0 |
|---|---|---|---|---|---|
| SRC-GSJ | 産総研 20 万分の 1 日本シームレス地質図 V2 | ✅ | ✅ | 必須 | 中核 |
| SRC-GSI | 国土地理院 地理院タイル / 地名検索 | ✅ | ✅(PDL1.0) | 必須 | 中核 |
| SRC-NABUNKEN | 全国遺跡報告総覧 OAI-PMH | ✅ | 索引は自由利用 | 必須 | 中核 |
| SRC-SOURCEAREA | 黒曜石原産地辞書(本プロジェクト作成) | — | ✅(本リポジトリ) | — | 中核 |
| SRC-ITOIGAWA | 糸魚川市 ヒスイ集成表 | ✅ | ❌ | — | 集計のみ |
| SRC-MEIJI-COLS | 明治大 黒耀石研究センター | ✅ | ❌ | — | 参照 |
| SRC-JPSEARCH | ジャパンサーチ | ❌ robots | — | — | **スコープ外** |

### 3.1 SRC-GSJ

- 凡例: `https://gbank.gsj.jp/seamless/v2/api/1.3.1/legend.json`
- 地点: 同 URL に `?point={lat},{lng}`(失敗形は §2.5)
- タイル: `https://gbank.gsj.jp/seamless/v2/api/1.3.1/tiles/{z}/{y}/{x}.png`(**y が先**)
- ライセンス: 政府標準利用規約(第 2.0 版)準拠。出典記載で改変を含む自由な二次利用が可能
- 出典表記: `20万分の1日本シームレス地質図V2(©産総研地質調査総合センター)`

### 3.2 SRC-GSI

- タイル: `https://cyberjapandata.gsi.go.jp/xyz/{std|pale}/{z}/{x}/{y}.png`(**x が先**)
- 地名検索: `https://msearch.gsi.go.jp/address-search/AddressSearch?q=`
- ライセンス: 公共データ利用規約(第 1.0 版)。出典「出典:国土地理院ウェブサイト」
- **地名検索の上位 1 件は正解ではない**(実測: `和田峠` の 1 位は青森県七戸町和田、
  `腰岳` の 1 位は沖縄の腰岳)。**候補生成にのみ使う**(§4.2)

### 3.3 SRC-NABUNKEN

- `https://sitereports.nabunken.go.jp/api/oai/request`
- robots.txt は `/ja/search*` 系のみ Disallow。`/api/oai/request` は許可されている
- 索引メタデータは自由利用。**報告書本文 PDF の著作権は発行自治体にあり V1.0 では触れない**

---

## 4. データモデル

構想書 §5 のエンティティのうち、V1.0 で実体を持つのは次のみ。

```text
SourceArea          原産地      … 黒曜石原産地辞書(自作・出典つき)
Geology             地質        … SourceArea へ GSJ から付与
Document            文献        … Nabunken OAI レコード
JadeAggregate       ヒスイ集計   … 糸魚川表からの集計統計のみ
LicenseManifest     権利情報
```

`Occurrence` / `Artifact` / `ProvenanceAnalysis` / `ModelPrediction` / `NetworkEdge` は
V1.0 では**実体を持たない**(§9 のスコープ外)。

### 4.1 SourceArea

```json
{
  "source_area_id": "OBS-WADATOGE",
  "material_id": "MAT-OBS",
  "name_ja": "和田峠",
  "aliases": ["和田峠群", "星ヶ塔", "星ヶ台"],
  "prefecture": "長野県",
  "latitude": 36.0,
  "longitude": 138.0,
  "coordinate_provenance": {
    "method": "gsi_address_search",
    "query": "和田峠",
    "candidate_index": 12,
    "candidate_title": "和田峠",
    "reviewed": true,
    "review_note": "..."
  },
  "evidence_status": "curated",
  "citations": ["CIT-0001"]
}
```

`coordinate_provenance` は**必須**。座標は必ず「どの問い合わせの、何番目の候補か」まで
記録する。手で書いた座標は `method: "manual"` とし、`citations` に典拠を必ず付ける。

### 4.2 座標の決め方(HC-012)

1. 地名を国土地理院 地名検索へ投げ、**候補を全部保存する**
2. 候補から選ぶのは人(このプロジェクトでは実装者)であり、
   **選んだ理由と候補番号を記録する**
3. 選べない・候補が 0 件の場合は `evidence_status: "needs_review"` のまま出荷し、
   画面でもそう表示する。**推測で埋めない**

---

## 5. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 黒曜石原産地辞書を、全件に出典と座標の出所を付けて構築する | must |
| F-02 | 各原産地へ GSJ の地質(symbol / formationAge / group / lithology)を付与する | must |
| F-03 | 対照群として、日本の陸域から無作為抽出した点にも同じ地質を付与する | must |
| F-04 | Nabunken OAI を全件収穫し、`setSpec` から市区町村コードを取り出す | must |
| F-05 | ヒスイ集成表を取得・正規化し、**集計統計のみ**を出荷形へ書き出す | must |
| F-06 | 地図: 地理院底図 + GSJ 地質重ね + 原産地点 + 県別ヒスイ集計 | must |
| F-07 | 原産地の詳細(Stone Passport): 地質・出典・根拠の階段・当該市町村の文献 | must |
| F-08 | 原産地の地質分布 vs 対照群の地質分布を統計で比べ、結果を図録に出す | must |
| F-09 | 「地質から原産地らしさを当てる」分類器を、地域で分けた交差検証で評価する | should |
| F-10 | XRF による原産地推定は **No-Go** を明記し、モデルカードのみ出す | must |
| F-11 | 全画面で根拠の階段(A〜E)を参照でき、AI 出力は必ず別表現で示す | must |
| F-12 | ライセンス頁で源ごとの到達・再配布・出典表示を実測引用つきで示す | must |

---

## 6. 出荷物

### 6.1 公開ファイル

```text
public/data/source_areas.json      黒曜石原産地(出典・座標出所・地質つき)
public/data/control_points.json    対照群(無作為陸地点 + 地質)
public/data/geology_stats.json     地質分布の比較と検定結果
public/data/documents_by_muni.json 市区町村別の文献件数
public/data/jade_aggregate.json    ヒスイ集計統計(**個票を含まない**)
public/data/licenses.json          権利情報
public/data/model_cards.json       モデルカード
```

### 6.2 同梱しないもの

- 糸魚川ヒスイ集成表の生ファイル・正規化個票(`data/raw/itoigawa/` は `.gitignore`)
- 明治大 COLS の PDF と、そこから抽出した表
- Nabunken の報告書 PDF 本文

### 6.3 ヒスイ集計の粒度

公開するのは**件数の集計のみ**とする。

```text
県別件数 / 時代別件数 / 種別件数 / 出土状況別件数 / 製作遺跡フラグの件数
```

遺跡名・所在市町村・文献などの**個票フィールドは公開ファイルに書き出さない**。
再現手順(1 コマンド)は画面と README に示す。

---

## 7. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | 公開ファイルの全レコードが、`config/sources.yaml` で `redistribution: true` の源に由来する | 陽性対照つきで検査 |
| G-02 | ヒスイの公開ファイルに個票フィールド(遺跡名・所在市町村・文献・所有者)が 1 個も無い | 陽性対照つきで検査 |
| G-03 | `SourceArea` 全件が `coordinate_provenance` と 1 件以上の `citations` を持つ | 全件検査 |
| G-04 | GSJ 取得器が §2.5 の 3 分岐を既知 4 地点で正しく判定し、**500 を再試行しない** | 固定地点の対照 |
| G-05 | ヒスイ解析器が、註の担当者一覧とシート一覧の食い違い(栃木の欠落・新潟の追加)を**例外ではなく明示された既知差分として**報告する | 実測値に一致 |
| G-06 | ヒスイの `種別` に埋まった個数(例「大珠1、垂玉2」)の和が `点数` と一致する行を数え、不一致を列挙する | 非循環の内部照合 |
| G-07 | 新潟の背景色つき 13 行と `点数` が BLANK の 13 行が完全に一致する | 非循環の内部照合 |
| G-08 | OAI 収穫器が 3 つのレコード形(oai_dc / junii2 / メタデータ無し)すべてを取り違えずに数える | 陽性対照つき |
| G-09 | 原産地 vs 対照群の地質分布の差を、順列検定で評価して報告する(**有意でなくても報告する**) | 実測 |
| G-10 | F-09 の分類器を地域で分けた `GroupKFold` で評価し、多数決ベースラインと並べる | 実測 |
| G-11 | 日本語本文に別字種・制御文字が無い(`harness/text_hygiene.py`) | 全件走査 |
| G-12 | 図の要素が `viewBox` に収まっている | `getBBox()` 実測 |
| G-13 | 出荷ビルド(`next build`)が通り、`.vercelignore` 適用後の木でも作れる | 実行 |
| G-14 | 全画面が複数の画面幅で横溢れしない | 実ブラウザ |
| G-15 | 出荷ファイルが実行ごとに変わる値を持たず、同じ入力・同じ種から二度作ってバイト列が一致する(構想書 AC-06) | 鍵の走査(陽性対照つき)+ 二度作って sha256 を比べる |

---

## 8. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 初期ロード < 3 MB gzip | 実測して SPEC に書き戻す |
| N-02 | Vercel 上で重い推論を実行しない(静的配信優先) | 関数 0 個 |
| N-03 | 外部取得はすべて Python 経由(HC-236) | 実装の走査 |
| N-04 | 長い取得はキャッシュ + 間を空けた再試行。「無い」は再試行しない(HC-221/235) | 対照テスト |
| N-05 | 日本語 UI / 出典表示は常時アクセス可能 | 実ブラウザ |

---

## 9. スコープ外(V1.0)

- 石材の**出土地点**を地図に出すこと(§2.1 により配れない)
- 報告書 PDF 全文からの NLP 抽出(構想書 §12)
- XRF による原産地推定モデルの実運用(§34 の No-Go に該当・F-10)
- GNN / 流通ネットワーク / 推定移動ルート(構想書 §15・§17)
- ジャパンサーチ連携(§2.2)
- 画像の再利用全般

---

## 10. 実測結果(L1・2026-09-09)

### 10.1 原産地辞書

| 項目 | 値 |
|---|---|
| 収録 | 14 件(全件で座標が確定) |
| 座標が確定 | 14 / 14 |
| 座標の精度が「概略」 | 2 件(白滝・秋保 — 露頭でなく集落・行政区域の地点) |
| 地質が取れた | 13 / 14(神津島 砂糠崎 は図郭にポリゴンが無い = 海岸の露頭) |
| 手で書いた座標 | **0 件** |

明治大 COLS は「国内に80数カ所」と述べており(`map01.html`)、
**本辞書の 14 件は網羅ではない**。差は「無い」のではなく「本辞書が確認できていない」。

**上位 1 件を正解にしていない**ことの実測:

| 問い合わせ | 候補数 | 採った候補 | 上位 1 件 |
|---|---|---|---|
| 和田峠 | 69 | **66 番目**(長野県) | 青森県七戸町和田 |
| 東餅屋 | 49 | 49 番目(長野県) | 北海道札幌市東区 |
| 白滝 | 117 | 1 番目(北海道) | 同じ |
| 柏峠 | 8 | 8 番目(静岡県) | 北海道遠軽町柏 |
| 砂糠崎 | 4 | 4 番目(東京都) | 埼玉県川越市砂 |

同名の峠が全国に 5 か所ある。**県コードで絞らなければ別地を掴む。**

### 10.2 姫島だけ規則を変えた(`title_is_exact`)

「姫島の黒曜石産地」は国土地理院の地名情報にその名で登録されているが、
その座標を逆ジオコードすると **None(陸上でない)**が返る ——
小島の海岸の露頭で、逆ジオコーダの陸域マスクが届いていない。

県コードの照合は**同名別地を切り分けるための**規則なので、切り分ける必要が
無いところでは要求しない。代わりに「完全一致がちょうど 1 件」という**一意性**で
確定させる。これが抜け道にならないことは対照で示してある ——
「和田峠」は完全一致が 5 件あるので一意性を満たさず、この経路には入れない(T-073)。

### 10.3 対照群

| 項目 | 値 |
|---|---|
| 陸マスク | GSJ シームレス地質図タイル z7・99 枚 |
| 陸画素 | 399,378 個(全 6,488,064 画素の 6.16%) |
| 純黒(境界線)の画素 | **0 個**(z7 では境界線が描かれない。z10 では 12.3%) |
| 抽出 | 400 点・重み `cos^2(緯度)`・seed 20260909 |
| 地質が取れた | 397 / 400(3 点は図郭にポリゴンが無い) |

**二経路の一致は 85.6%(340/397)** で、100% ではない。診断した結果、
ラスタの一般化によるものだった —— 不一致 57 件の行き先が系統的で、多くが
`H_sad`(谷底平野・山間盆地・河川・海岸平野堆積物)= 幅の狭い線状の単位である。
z7 の 1 画素は約 1 km なので、細い谷は周囲の岩体に塗り込められる。
大分類レベルでは 57 件中 40 件(70%)が一致する。

**結論には影響しない。** 出荷レコードの地質はタイルではなく点の問い合わせ API から
取っており、タイルは「どこが陸か」を決めるためだけに使っている。
原産地も同じ API で測るので、両群の計器は揃っている。

### 10.4 主張の判定

**判定の規則を先に決めた**: 宣言した感度分析の**すべての条件で** p < 0.05 のときだけ
「成立」とする。一つの切り方で有意になり別の切り方で有意でなくなる主張は
「不成立」と書く。

| ID | 主張 | 判定 |
|---|---|---|
| H-01 | 原産地の地質は火成岩に偏る | **成立** |
| H-02 | 第四紀の火成岩に絞るとさらに強い | **成立** |
| H-03 | 珪長質火山岩(デイサイト・流紋岩)に偏る | **不成立** |

```text
H-01  全件            92.3% (n=13) vs 39.5% (n=397)  p = 0.00010
      面積精度を除く  100.0% (n=11) vs 39.5% (n=397)  p = 0.00005

H-02  全件            69.2% (n=13) vs 11.3% (n=397)  p < 0.00005
      面積精度を除く   81.8% (n=11) vs 11.3% (n=397)  p < 0.00005

H-03  細分のみ        38.5% (n=13) vs  3.5% (n=397)  p = 0.00020
      まとめる        38.5% (n=13) vs 13.6% (n=397)  p = 0.02895
      まとめる・除く   36.4% (n=11) vs 13.6% (n=397)  p = 0.05785  ← 0.05 を超える
```

**H-03 が落ちた理由は解釈できる。** 20 万分の 1 の図郭は黒曜石の岩体より粗い。
原産地 13 件のうち珪長質火山岩の label を持つのは 5 件で、6 件は
安山岩・玄武岩質安山岩と出る。**個々の label は黒曜石そのものの岩相ではない** ——
「安山岩」と出た原産地に黒曜石が無いのではなく、図郭が分離できていない。

黒曜石は流紋岩質のガラスなので、H-03 は本来もっとも成り立ってほしい主張だった。
それが計器の解像度で落ちたことは、この地質図で個々の原産地の岩相を語れないことを
意味する。**大分類(H-01)と年代(H-02)までが、この計器で言えることの限界である。**

---

## 11. 実測結果(L2 ヒスイ・2026-09-10)

配っているのは**集計だけ**である。個票フィールド(遺跡名・所在市町村・文献・
所有者・その他・伴出土器)は出荷形に 1 個も現れない —— 書き出す前に機械で確かめ、
出荷済みのファイルにも当てている(G-02)。

### 11.1 収録

| 項目 | 値 |
|---|---|
| データシート | 9(9 県) |
| データ行 | 572 |
| 点数が数値で入っている行 | 343 |
| 記録された点数の和 | 1,448 |

`点数` セルの型: 数値 343 / 空欄 215 / 文字列 1(`６？`)/ 書式のみ 13。
**空欄は 0 ではなく不明**(註 2)なので、和 1,448 は 572 行の合計ではない。

### 11.2 三つの内部照合

いずれも**源の中だけで成り立つ**ので循環しない。

#### G-05 註が挙げる県 と 実際のシート — 食い違いあり

| | 件数 |
|---|---|
| 註が調査担当者を挙げる県 | 9 |
| 実際のデータシート | 9 |

**件数が同じなので、件数だけでは食い違いが見えない。** 集合で比べると:

- 註にあってシートが無い: **栃木県**
- シートにあって註に無い: **新潟県(上・中越)**

#### G-06 種別の内訳の和 と 点数の欄 — 食い違い 5 件

| 項目 | 件数 |
|---|---|
| 照合できた | 34 |
| 一致 | 29 |
| **食い違い** | **5** |
| 読めなかった | 1 |
| 全角数字のみ(個数ではない) | 1 |

**記法がセルごとに二形ある。**

- 形 A ―― `勾玉(2),小玉(1)` 括弧の中が個数(北海道のシート)
- 形 B ―― `大珠1、垂玉2` 裸の数字が個数、括弧は注記(青森のシート)

どちらかはセルの全部分が同じ形に当たるかで決め、**どちらでもないものは
推測せず読まない**。括弧は個数のことも注記のこともある ——
`勾玉3(未成品2)` は勾玉 3 点であって 5 点ではない。

食い違った 5 行(記法のみ・すべて青森のシート):

```text
勾玉1、垂玉1   内訳 2 vs 点数 1
勾玉3(未成品2)  内訳 3 vs 点数 1
垂玉2          内訳 2 vs 点数 1
勾玉1,丸玉2    内訳 3 vs 点数 1
勾玉3,管玉2    内訳 5 vs 点数 3
```

**探索時の数(照合 31 / 一致 26 / 解析不能 5)と違う。** 探索の規則をそのまま
写さず、二点直して昇格させたためである(HC-069)——(a) 形 A に対応して
北海道の 3 行が照合へ移り 3 行とも一致した、(b) 全角数字を個数と見ないようにして
`樽型大珠・３孔` が対象外になった。**食い違いの 5 行は両方で同一**で、
規則を直しても動かない。それが「源の中の食い違い」である証拠になっている。

なお `Python の \d は全角数字にも当たる`。`[0-9]` を明示しないと
`３孔`(穿孔の数)を個数として読む。

#### G-07 背景色つきの行 と 点数が「書式のみ」の行 — 完全一致

| | 行数 |
|---|---|
| 背景色つき | 13 |
| 点数セルが BLANK(書式あり・値なし) | 13 |
| 一致 | **完全一致** |

対象は新潟のシートのみ。**書式とセルの型は別の経路で決まる**のに同じ 13 行を
指す —— 行全体に色を塗ったので、空の点数セルにも書式が付いた。

註は「緑の欄」と書くが、実体は **RGB(255,255,153) の淡い黄色**である。
**註と書式も食い違っている**ので、色の名前で探してはならない。

13 行の意味(製作遺跡)は、書式だけでなく**別のフィールド**からも裏づけた ——
13 行すべての `種別` に「未成品」「原石」「敲石」「角礫」のいずれかが現れる。

### 11.3 語彙の分布

「不明」と空欄は語彙に数えないが、**件数は出す**。落とした行を消すと
「そういう行が無かった」と読めてしまう。

| 列 | 異なり | 記入あり | 「不明」 | 空欄 |
|---|---|---|---|---|
| 時代 | 8 | 513 | 2 | 57 |
| 時期 | 85 | 491 | 18 | 63 |
| 種別 | 120 | 545 | 1 | 26 |
| 形状 | 21 | 95 | 0 | 477 |
| 出土状況 | 141 | 304 | 15 | 253 |
| 石材 | 3 | 115 | 0 | 457 |

三つの数の和は必ず 572 になる(検査で固定してある)。
`形状` と `石材` はそれぞれ 477 / 457 行が空欄で、**3 県のシートにしか列が無い**。

---

## 12. G-10 の事前宣言(L3・2026-09-14、**モデルを一度も走らせる前に書いた**)

### 12.1 問い

**学習した組み合わせ(ロジスティック回帰)は、訓練側で選んだ単一規則を超えるか。**

H-01〜H-03 と同じデータ・同じ計器(GSJ 点の問い合わせ)から作る特徴なので、
分類器が当たることは H の確認にならない(HC-045)。問いはこの一点に絞る。

### 12.2 手順

| 項目 | 宣言 |
|---|---|
| 正例 | 原産地のうち地質が取れた点(13 件) |
| 負例 | 対照群のうち地質が取れた点(397 点) |
| 特徴 | 地質の大分類 5 / 年代 5(第四紀・新第三紀・古第三紀・白亜紀・それ以前)/ 岩相 4(珪長質・火山岩・深成岩・未固結堆積物)の二値 |
| 分割 | **1.5 度格子の一群抜き**(原産地と対照点を同じ格子で束ねる) |
| 比較 | 多数決 / **訓練側だけで選ぶ**単一規則 / ロジスティック回帰(`class_weight=balanced`) |
| 指標 | 全折りの予測を束ねた平衡正解率と、件数(TP・FN・FP・TN) |
| 対照 | ラベル置換 200 回(種 20260914)でロジスティックの置換 p 値 |

### 12.3 判定

- **G-10 成立** ―― ロジスティックの平衡正解率が、単一規則より **0.05 以上**高い
- **G-10 不成立** ―― それ以外。画面では単一規則のほうを示し、学習モデルは「超えなかった」と書く

いずれの場合も、置換 p 値が 0.05 以上なら「この特徴では原産地を見分けられない」と書く。

**閾値は結果を見て動かさない。** 正例 13 件の平衡正解率は 1 件で約 3.8 ポイント動くので、
0.05 の差は正例 1〜2 件ぶんにすぎない —— この粒度も結果と一緒に書く。

### 12.4 訓練しないもの

- **PyTorch MLP** ―― 正例 13 件では、表現学習が単一規則を超える余地の検証自体が成り立たない。
  構想書 §13.8 の出口(UI を無効化しモデルカードのみ)を使う
- **異常検知(Isolation Forest / AutoEncoder)** ―― 「周辺と比べて特徴が異なる」を
  言うための個票(出土資料)を配れないので、対象が存在しない(§2.1)

### 12.5 結果(2026-09-14・種 20260914・置換 200 回)

同じ条件で二度走らせ(探索用の走査と出荷器)、全ての数が一致した(所要 158.1 秒 / 142.6 秒)。

| 方法 | 平衡正解率 | 原産地を当てた | 対照点を誤って原産地とした |
|---|---|---|---|
| 多数決 | 0.5000 | 0 / 13 | 0 / 397 |
| 訓練側で選んだ単一規則 | 0.8406 | 12 / 13 | 96 / 397 |
| ロジスティック回帰 | 0.8646 | 12 / 13 | 77 / 397 |

- 正例 13 / 負例 397 / 群 40
- 単一規則は **40 折すべてで `volcanic`**(岩相に「溶岩」か「火砕」を含む)が選ばれた
- ロジスティック回帰の pooled AUC 0.8553
- 置換 200 回: 平均 0.4943 / 最大 0.7600 / **p = 0.0050**

**G-10 不成立。** 差 +0.0239 は宣言した 0.05 に届かない。

差はすべて**偽陽性の減少**(96 → 77 = 19 点)に出ており、原産地を当てた数は
両者とも 12/13 で同じである。0.0239 は 19/397 × 0.5 と一致する。

**見分けられること自体は成り立つ**(置換 p = 0.0050)。つまり ——
地質の特徴で黒曜石原産地を無作為な陸地点から見分けられるが、
**「足もとが火山岩(溶岩・火砕岩)か」の一つの規則で足り、学習した組み合わせは
それを宣言した幅では超えなかった。**

この結果は H-01(火成岩に偏る)とも整合するが、同じデータ・同じ計器なので
その確認にはならない(§12.1)。

### 12.6 出荷物の再現性(G-15・2026-09-14)

所要時間を出荷形から外したうえで、同じ入力・同じ種(20260914)から
`model_cards.json` を二度作り、バイト列を比べた。

| 実行 | sha256 | 大きさ |
|---|---|---|
| 一度目(`public/data/`) | `869128376735511236a6e48b0d4e4b35bfc00f9e7d20ec7bd7350fd179a2942c` | 7,559 B |
| 二度目(別の出力先) | `869128376735511236a6e48b0d4e4b35bfc00f9e7d20ec7bd7350fd179a2942c` | 7,559 B |

**一致した。** 修正前は所要時間 `evaluation_seconds`(158.1 秒 / 142.6 秒)を載せていたため、
数がすべて一致していても出荷物のバイト列が毎回変わっていた。

G-10 の数は、探索・出荷器・再現性確認の**三度とも**同じだった
(平衡正解率 0.5000 / 0.8406 / 0.8646・差 +0.0239・置換 p 0.0050)。
